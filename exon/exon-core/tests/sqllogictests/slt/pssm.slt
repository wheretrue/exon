statement ok
CREATE FUNCTION pssm(DNA VARCHAR)
RETURNS FLOAT
LANGUAGE jaspar16
AS '$CARGO_MANIFEST_DIR/test-data/models/jaspar/MA0001.3.pfm';

query I
SELECT ROUND(pssm('GTTGACCTTATCAAC'), 2);
----
-43.33

query I
SELECT ROUND(pssm(s1), 2) FROM (SELECT 'GTTGACCTTATCAAC' s1) t
----
-43.33

statement ok
CREATE FUNCTION pssm(DNA VARCHAR)
RETURNS FLOAT
LANGUAGE transfac
AS '$CARGO_MANIFEST_DIR/test-data/models/transfac/M00005.transfac';

query I
SELECT ROUND(pssm('AGAACCAGCTGTGGAATG'), 2);
----
20.64

query I
SELECT ROUND(pssm(s1), 2) FROM (SELECT 'AGAACCAGCTGTGGAATG' s1) t
----
20.64
